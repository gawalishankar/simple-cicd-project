name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Build and Push Frontend
      run: |
        docker build -t yourdockerhub/frontend:latest -f Dockerfile-frontend .
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
        docker push yourdockerhub/frontend:latest

    - name: Build and Push Backend
      run: |
        docker build -t yourdockerhub/backend:latest -f Dockerfile-backend .
        docker push yourdockerhub/backend:latest

    - name: Deploy to Kubernetes
      run: |
        kubectl apply -f k8s/
      env:
        KUBECONFIG: ${{ secrets.KUBECONFIG }}
